
// System Libraries
#include <iostream>

#include "core/logging.h"
#include "multirotor/multirotor_simulator.h"

int main(int argc, char **argv)
{
    MultirotorSimulator sim;
    // VERIFY(sim.Init());
    // VERIFY(sim.Run());

    return 0;
}
